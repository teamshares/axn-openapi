# frozen_string_literal: true

source "https://rubygems.org"

# The gem under test — pulls in its own axn dependency and any Railtie/Engine it defines.
gem "axn-openapi", path: "../../"

gem "rails", "~> 7.0"
gem "sqlite3", "~> 2.0"

group :development, :test do
  gem "rspec", "~> 3.0"
end
